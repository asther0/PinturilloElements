"use client";

export default function DrawingTools() {
  // This component is a placeholder wrapper that is conditionally rendered
  // The actual tool UI lives inside GameCanvas for proximity to the canvas context
  return null;
}
